export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};

// When building your Angular application, specify the target environment
// to do so use the --configuration flag with the Angular CLI commands.
// Example: ng build --configuration=production
